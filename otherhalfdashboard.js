/*
                if( nxtwd == today && od < today ){
                    $('#todayToDo').append('<tr class="displayRow" id="todayToDo' + uid +'"></tr>');
                            $('#todayToDo' + uid).append(`
                            <th>${odCB}</th><td>${pnn}</td><td>${pv}</td><td>${psn}</td><td>${pt}</td><td>${fq}</td><td>${lwd}</td>
                            `);
                } else if( nwd < today && od < today ){
                    $('#pastDue').append('<tr class="displayRow" id="pastDue' + uid +'"></tr>');
                            $('#pastDue' + uid).append(`
                            <th></th><td>${pnn}</td><td>${pv}</td><td>${psn}</td><td>${pt}</td><td>${fq}</td><td>${lwd}</td>
                            `);
                } else{

                };

                // conditional statements to execute list
            });
        }
    });
*/
/*
    load created function (from success)

    update list when user clicks "mark as done"
    $('#aP01 .cB').click(function(event){
        e.preventDefault();
        // 1. AJAX need to send updated data to php
        $.ajax({
            url: 'dashboard.php',
            type: 'POST',
            data: {},
            cache: false,
            contentType: false,
			processData: false,
            success: function (){
                // display updated dashboard
                    // remove this from its parent (div)
                    // push this into "completed today" div
            };

        });
    });
*/